{
  "task": "Update Database Schema for Referrals",
  "files_to_modify": ["auth-server/src/db.js"],
  "instructions": {
    "target_table": "users",
    "actions": [
      "Add a column 'referral_code' (VARCHAR(20), UNIQUE) to store the user's unique invite code.",
      "Add a column 'referred_by' (INTEGER, REFERENCES users(id)) to track who invited the user.",
      "Update the INIT_SQL string to include these ALTER TABLE statements or modify the CREATE TABLE definition."
    ],
    "logic": "Generate the 'referral_code' automatically during user creation if not provided, or ensure the column exists for existing users."
  }
}
{
  "task": "Implement Referral Logic in Auth Routes",
  "files_to_modify": ["auth-server/src/routes/auth.js"],
  "instructions": {
    "endpoint": "POST /api/auth/register",
    "modifications": [
      "Extract 'referralCode' from req.body.",
      "Before inserting a new user, check if the provided 'referralCode' exists in the database to find the 'referrerId'.",
      "Generate a unique 6-8 character alphanumeric 'referral_code' for the new user.",
      "Update the INSERT query to include 'referral_code' and 'referred_by' fields.",
      "Optional: If a valid referrer is found, increment their 'balance' in the users table as a reward."
    ]
  }
}{
  "task": "Add Referral Code Field to RegisterForm",
  "files_to_modify": ["auth-client/src/components/RegisterForm.jsx"],
  "instructions": {
    "ui_changes": [
      "Add a new state variable 'referralCode' using useState.",
      "Add an input field for 'Referral Code' (optional) above the submit button.",
      "Ensure the input field is styled consistently with other form-inputs."
    ],
    "functionality": [
      "Update the 'handleSubmit' function to include 'referralCode' in the JSON body of the fetch request to /api/auth/register."
    ]
  }
}
{
  "task": "Display Referral Link on Dashboard",
  "files_to_modify": ["auth-client/src/components/Dashboard.jsx"],
  "instructions": {
    "ui_changes": [
      "Locate the user profile or account section.",
      "Display the user's 'referral_code' fetched from the backend (ensure the /me endpoint returns this field).",
      "Add a 'Copy Referral Link' button that copies 'http://localhost:5173/register?ref={code}' to the clipboard."
    ],
    "enhancement": "If possible, display a count of successful referrals by querying the users table where 'referred_by' equals current user ID."
  }
}